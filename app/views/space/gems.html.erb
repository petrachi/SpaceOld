<%= container do %>
	<%= one_col_row do %>
		<h2><%=t :'gems.name' %></h2>
	<% end %>
	
	<%= row do %>
		<%= ten_span do %>
			<%=t :'gems.description' %>
		<% end %>

		<%= two_span :align => :right do %>
			<% if authorized? :gems %>
				<%= link_to t(:go_to), root_url(:subdomain => :gems), :class => :btn %>
			<% end %>
			
			<% if install_authorized? :gems %>
				<%= link_to t(:install), install_url(:subdomain => :gems), :class => :btn %>
			<% end %>
		<% end %>
	<% end %>
<% end %>
