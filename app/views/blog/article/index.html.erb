<%= two_cols_container :collection => @articles do |article| %>
	<h2 align="center">
		<%= article.title %>
	</h2>
	
	<p>
		<%= article.summary.html_safe %>
	</p>
	
	<p align="center">
		<%= link_to t(:discover), article_url(:action => :show, :id => article.id), :class => :btn %>
	</p>
<% end %>
