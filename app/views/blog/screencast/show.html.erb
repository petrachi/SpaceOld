<%= container do %>
	<%= row do %>
		<%= three_span :element => :sidebar do %>
	
			<h3><%=t 'blog.screencast.pool' %></h3>
		
			<p>
				<%= link_to @screencast.pool, @screencast.pool_url, :class => :btn %>
			</p>
		<% end %>
	
		<%= nine_span :element => :article do %>
			<h2>
				<%= @screencast.title %>
			</h2>
			
			<%= erb @screencast.embed %>
			
			<h3><%= t 'blog.screencast.ressources' %></h3>
			
			<%= erb @screencast.run %>
		<% end %>
	<% end %>
<% end %>
