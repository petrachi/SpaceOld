<%= one_col_container do %>
	<%= link_to "voir ton cv finis", {:action=>:show, :id=>current_user.id} %>
	
	<%= link_to "crée un nouveau achievement", {:action=>:edit} %>
<% end %>

<%= container do %>
	<% @achievements.each do |achievement| %>
		<%= row do %>
			<%= two_span do achievement.year end %>
			<%= seven_span do "#{ achievement.activity } - #{ achievement.brief }" end %>
			<%= three_span do %>
				<%= link_to "Edit", {:action=>:edit, :id=>achievement.id} %>
				<%= link_to "Delete", {:action=>:destroy, :id=>achievement} %>
			<% end %>
		<% end %>
	<% end %>
<% end %>